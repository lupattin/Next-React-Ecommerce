import { TextField } from "@mui/material";

export const SearchBar = () => {
    return (
      <div>
        
        <TextField fullWidth= {false} label="fullWidth" id="fullWidth" />
      </div>
    );
  };