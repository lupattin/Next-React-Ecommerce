import { atom } from "recoil";

export const signInDisplay = atom({
    key: "signInDisplay",
    default: "flex"
  });
export const codeDisplay = atom({
    key: "codeDisplay",
    default: "none"
  });